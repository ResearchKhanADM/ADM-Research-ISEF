# 009 · merged into 008

*Date:* 2026-08-15 · *Status:* superseded before it existed

`dS/dt` and the death hazard were originally planned as two decision files. They
are one decision — the hazard is what replaces the deleted threshold on `S`, and
splitting them would have produced two files that only make sense read together.
Both are in **`008-viability-hazard.md`**.

This stub exists because the numbering is referenced from git history, the
master plan and `docs/PROGRESS.md`. **Do not renumber 010 or 011 to close the
gap** — a missing 009 with no explanation reads as a lost file, which is worse
than a gap with a note.
